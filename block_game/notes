getElementsByClassName - не трогать, не понятно, как оно работает, пользоваться getElementById

--------------------------
addEventListener - начать разбиратсья с этим
Если в DOM не найеден елемент то возвращается null
разница между вызовом foo() и foo